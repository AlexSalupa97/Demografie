<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.demografie">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitati.Activitati.TestActivity"></activity>
        <activity
            android:name=".Activitati.Activitati.VizualizareFragmenteActivity"
            android:screenOrientation="portrait"
            android:label="Prelucrare date"/>
        <activity
            android:name=".Activitati.Activitati.MainActivity"
            android:screenOrientation="portrait"
            android:label="Prima pagina"/>
        <activity
            android:name=".Activitati.Activitati.SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>